This directory contains configuration files for GTK 3.0 applications. These files are used to customize the appearance and behavior of GTK 3.0 applications on your system.
For now this only modifies the bookmarks in the file manager. You can add bookmarks by creating a file named "bookmarks" in this directory and adding lines in the following format:

For the bookmarks file:
 - The first part is the URI scheme (file://), followed by the absolute path to the directory or file. 
 - The name after the last slash is the display name for the bookmark.
 