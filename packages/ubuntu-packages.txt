# ubuntuRicePack desktop packages
# Targets: Ubuntu 25.04 (Plucky/GNOME 48) and Ubuntu 26.04 LTS
# (Resolute/GNOME 50)
# This list contains the shared CLI inventory plus desktop packages explicitly
# verified by the final UbuntuRicePack audit.
#
# This is a release-spanning superset. The installer checks the active APT
# index and installs only packages available for the detected release. It
# ignores blank lines and lines beginning with '#'.
#
# Required Ubuntu components: main, universe, multiverse.
# Policy: CLI-focused, no Snap packages.
#
# Important command-name differences to handle later in the terminal script:
#   Arch `bat` -> Ubuntu package `bat`, command `/usr/bin/batcat`
#   Arch `fd`  -> Ubuntu package `fd-find`, command `/usr/bin/fdfind`
# `tealdeer` supplies the `tldr` command.

# ---------------------------------------------------------------------------
# Base and build tools
# Arch: base-devel -> build-essential
# Arch: python -> python3
build-essential
git
curl
wget
unzip
zip
gdb
python3
python3-pip
python3-venv
less
sudo
nano
tree
ca-certificates
gnupg
bash-completion
pkg-config
procps

# ---------------------------------------------------------------------------
# pyenv and CPython build dependencies
# Arch runtime/development package translations:
#   openssl  -> openssl + libssl-dev
#   zlib     -> zlib1g-dev
#   xz       -> xz-utils + liblzma-dev
#   bzip2    -> bzip2 + libbz2-dev
#   libffi   -> libffi-dev
#   readline -> libreadline-dev
#   sqlite   -> sqlite3 + libsqlite3-dev
#   tk       -> tk-dev
#   ncurses  -> libncurses-dev
pyenv
openssl
libssl-dev
zlib1g-dev
xz-utils
liblzma-dev
bzip2
libbz2-dev
libffi-dev
libreadline-dev
sqlite3
libsqlite3-dev
tk-dev
libncurses-dev
libxml2-dev
libxmlsec1-dev

# ---------------------------------------------------------------------------
# Modern command-line tools
# Arch: fd -> fd-find
# Arch: tldr -> tealdeer
# Arch: p7zip + 7zip -> 7zip (p7zip was superseded)
btop
htop
eza
bat
fd-find
ripgrep
fzf
zoxide
jq
ncdu
tealdeer
chafa
7zip
unrar

# ---------------------------------------------------------------------------
# Shell, prompt, and system information
zsh
starship
fastfetch

# ---------------------------------------------------------------------------
# File transfer and remote access
rsync
sshfs
smbclient
magic-wormhole

# ---------------------------------------------------------------------------
# Containers
# Arch: docker -> docker.io
# Arch: docker-compose -> docker-compose-v2
# Native services require systemd enabled in /etc/wsl.conf. If Docker Desktop
# integration owns the daemon, the later installer should avoid starting a
# second Docker daemon inside WSL.
docker.io
docker-compose-v2

# ---------------------------------------------------------------------------
# Image handling and small terminal utilities
imagemagick
fontconfig
sl
cmatrix

# ---------------------------------------------------------------------------
# Desktop integration and optional VLC feature modules
gnome-browser-connector
gnome-screenshot
sxhkd
vlc
vlc-l10n
vlc-plugin-access-extra
vlc-plugin-base
vlc-plugin-fluidsynth
vlc-plugin-jack
vlc-plugin-notify
vlc-plugin-pipewire
vlc-plugin-qt
vlc-plugin-samba
vlc-plugin-skins2
vlc-plugin-svg
vlc-plugin-video-output
vlc-plugin-video-splitter
vlc-plugin-visualization

# ---------------------------------------------------------------------------
# Deliberately not installable from this APT-only list
#
# ollama
#   No package exists in Ubuntu's archive. Use Ollama's upstream Linux
#   installer in the dedicated local-AI script, and only after deciding whether
#   Ollama should run inside WSL or on the Windows host.
#   Official instructions: https://docs.ollama.com/linux

# ---------------------------------------------------------------------------
# GNOME 48/50 desktop, appearance, and configuration
gdm3
gnome-session
gnome-shell
gnome-shell-extensions
gnome-shell-ubuntu-extensions
gnome-control-center
gnome-settings-daemon
gnome-system-monitor
gnome-tweaks
gnome-software
gnome-keyring
gnome-shell-extension-manager
dconf-cli
dconf-editor
libglib2.0-bin
desktop-file-utils
shared-mime-info
libnotify-bin
zenity

# Ubuntu 25.04 and 26.04 provide Ptyxis and xdg-terminal-exec. The latter makes
# the same terminal choice available to Ctrl+Alt+T and "Open in Terminal".
ptyxis
xdg-terminal-exec

# ---------------------------------------------------------------------------
# GTK, portals, and the exact icon theme selected by the GNOME configuration
libgtk-3-0t64
libgtk-3-bin
libgtk-4-1
libadwaita-1-0
gtk-update-icon-cache
sassc
xdg-desktop-portal
xdg-desktop-portal-gnome
xdg-desktop-portal-gtk
xdg-user-dirs
xdg-user-dirs-gtk
xdg-utils
papirus-icon-theme

# ---------------------------------------------------------------------------
# Nautilus, storage, filesystems, archives, and file sharing
nautilus
python3-nautilus
nautilus-image-converter
nautilus-share
gnome-sushi
gvfs
gvfs-backends
gvfs-fuse
udisks2
ntfs-3g
exfatprogs
dosfstools
mtools
gnome-disk-utility
gparted
file-roller

# ---------------------------------------------------------------------------
# Desktop applications, documents, images, and media
qbittorrent
audacious
audacious-plugins
celluloid
mpv
gthumb
loupe
ffmpegthumbnailer
baobab
papers
gsimplecal
flameshot

# ---------------------------------------------------------------------------
# Audio, Bluetooth, power, input, and X11 compatibility
alsa-utils
blueman
bluez
brightnessctl
acpi
linux-tools-generic
pamixer
pavucontrol
playerctl
pipewire
pipewire-pulse
wireplumber
power-profiles-daemon
keyd
xcape
xclip
xdotool
wmctrl
x11-xserver-utils
xwayland

# ---------------------------------------------------------------------------
# Networking, kernel, firmware, graphics, and boot support
network-manager
network-manager-applet
linux-generic
linux-firmware
intel-microcode
libgl1-mesa-dri
mesa-vulkan-drivers
efibootmgr
os-prober
grub2-common

# ---------------------------------------------------------------------------
# Fonts used by the rice. The three patched Nerd Font archives are installed
# and SHA-256 verified by stage 03.
fonts-noto-core
fonts-noto-extra
fonts-noto-cjk
fonts-noto-color-emoji
fonts-noto-mono
fonts-dejavu-core
fonts-liberation
fonts-ubuntu
fonts-jetbrains-mono
fonts-powerline
fontconfig
